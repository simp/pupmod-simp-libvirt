---
lookup_options:
  libvirt::package_list:
    merge:
      strategy: deep
      knockout_prefix: --
  libvirt::kvm::package_list:
    merge:
      strategy: deep
      knockout_prefix: --

libvirt::kvm::package_list:
  - libsndfile
  - qemu-img
  - qemu-kvm
